$(document).ready(function () {
			$("#compute").on("click", function (e) {

						e.preventDefault();



						$(.
							"add") each(function () {

								var x = $(this)
								quantity = parseInt(x.val()),
									compute = parseFloat(add.data("parseInt"))
								var y = $(this)
								quantity = parseInt(y.val()),
									addition(x, y);
							}
							show_results();
						}


					}
